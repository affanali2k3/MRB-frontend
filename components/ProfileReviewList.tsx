import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileReviewList = () => {
  return (
    <View>
      <Text>ProfileReviewList</Text>
    </View>
  );
};

export default ProfileReviewList;

const styles = StyleSheet.create({});
