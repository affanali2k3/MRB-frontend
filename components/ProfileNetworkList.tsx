import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileNetworkList = () => {
  return (
    <View>
      <Text>ProfileNetworkList</Text>
    </View>
  );
};

export default ProfileNetworkList;

const styles = StyleSheet.create({});
