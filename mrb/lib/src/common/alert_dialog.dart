// import 'package:flutter/material.dart';

// Future<dynamic> showCustomDialog(BuildContext context, Widget content, List<dynamic> comments) {
//   return showDialog(
//     context: context,
//     builder: (context) {
//       return AlertDialog(
//         content: SingleChildScrollView(child: ListView.builder(itemCount: comments.length ,itemBuilder:(context, index) {
//           return Text(data)
//         },),),
//       );
//     },
//   );
// }
